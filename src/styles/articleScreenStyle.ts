import { StyleSheet } from 'react-native';

const articleScreenStyle = StyleSheet.create({
  container: {
    alignItems: 'center',
    justifyContent: 'center'
  }
});

export default articleScreenStyle;
